package com.guardvillagers.client;

/**
 * Debug overlay for guard patrol ranges, home zones, and target lines.
 * Currently stubbed out — the 3D world-rendering pipeline changed in 1.21.11
 * and needs a full port to the new API before this can be re-enabled.
 */
public final class GuardDebugRenderer {
	private GuardDebugRenderer() {}

	public static void register() {
		// no-op until ported to the new rendering API
	}
}
